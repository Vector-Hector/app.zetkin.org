import { SafeRecord } from 'utils/types/safeRecord';

export const inlineVars: SafeRecord<string, string> = {
  'target.first_name': 'First name',
  'target.full_name': 'Full name',
  'target.last_name': 'Last name',
};
